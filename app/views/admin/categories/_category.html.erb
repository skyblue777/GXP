<tr>
  <td><%= category.id %></td>
  <td><%= category.name %></td>
  <td class="text-right">
    <%= link_to 'Edit', edit_admin_category_path(category) %>
    &nbsp; / &nbsp;
    <%= link_to 'Delete', admin_category_path(category), method: :delete, data: { confirm: 'Are you sure you want to delete the selected Category?' } %>
  </td>
</tr>
